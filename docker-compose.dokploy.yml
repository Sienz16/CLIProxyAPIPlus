services:
  cli-proxy-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-6.8.53}
        COMMIT: ${COMMIT:-local}
        BUILD_DATE: ${BUILD_DATE:-2026-03-15T00:00:00+08:00}
    container_name: cli-proxy-api
    environment:
      DEPLOY: ${DEPLOY:-cloud}
      TZ: ${TZ:-Asia/Kuala_Lumpur}
      MANAGEMENT_PASSWORD: ${MANAGEMENT_PASSWORD:-}
    ports:
      - "${CLI_PROXY_PORT:-8317}:8317"
    volumes:
      - cli-proxy-config:/CLIProxyAPI/config
      - cli-proxy-auth:/root/.cli-proxy-api
      - cli-proxy-logs:/CLIProxyAPI/logs
    restart: unless-stopped
    command:
      - /bin/sh
      - -c
      - >-
        mkdir -p /CLIProxyAPI/config &&
        if [ ! -s /CLIProxyAPI/config/config.yaml ]; then
          cp /CLIProxyAPI/config.example.yaml /CLIProxyAPI/config/config.yaml;
        fi &&
        exec ./CLIProxyAPIPlus -config /CLIProxyAPI/config/config.yaml

volumes:
  cli-proxy-config:
  cli-proxy-auth:
  cli-proxy-logs:
